we need more agents! the subagents and orchestrators need definitions. we only have a ralph loop agent now - the ralph loop should call the orchestrator agent that calls the sub agents that run the ralph loops. each agent gets its own role file. if you need help defining these, you should do some research and find out what works well and what doesnt, look at some examples online.

continue
