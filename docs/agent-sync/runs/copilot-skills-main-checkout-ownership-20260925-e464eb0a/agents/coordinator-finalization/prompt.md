Finalize the preserved Ralph main-checkout-ownership implementation after the iteration-stall task's recorded dashboard scope release. Claim only this run's coordinator leaf, progress, decision records, and docs/ralph-status.md before editing. Index the missing ownership leaf without deleting or rewriting other runs; preserve the resource-manager changes already on remote main and the separate role-hierarchy agent's unmerged work. Rebase the isolated ownership worktree onto the latest fetched remote main, run relevant complete Ralph contracts, then acquire a short-lived MERGE main reservation and integrate through the repository's permitted path. Verify the remote-main result, release main promptly, publish this task's sign-out, and perform the required post-merge memory review. Never use the shared main checkout for routine refresh or force another agent's scope release.
