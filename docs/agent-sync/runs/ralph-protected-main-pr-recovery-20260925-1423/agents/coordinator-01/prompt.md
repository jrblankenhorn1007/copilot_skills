Update canonical Ralph Loop and agent-sync guidance for a verified GH013 denial that explicitly requires GitHub API/UI merging. Define a bounded, policy-compliant PR-only status recovery; never retry a forbidden direct write, force-push, use an admin bypass, or claim sign-in until the status merge is verified on remote main. Keep generic authentication or permission failures blocked. Clarify status and merge reservation handling and what to do if required PR checks or review cannot proceed. Change only the Ralph Loop skill and agent-sync README/main-ownership guidance, validate the documentation, and integrate through the normal reviewed PR process.
